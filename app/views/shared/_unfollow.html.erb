
<%= form_for(current_user.relationships.find_by_followed_id(@chef.id),
             html: { method: :delete },
             remote: true) do |f| %>
  <div class = "lightgray_btn"><%= f.submit "UNFOLLOW"%></div>
<% end %>
