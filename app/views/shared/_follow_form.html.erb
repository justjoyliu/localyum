<% unless current_user?(@chef) %>
  <div id="follow_form">
	  <% if current_user.following?(@chef) %>
	    <%= render 'shared/unfollow' %>
	  <% else %>
	    <%= render 'shared/follow' %>
	  <% end %>
  </div>
<% end %>