<%= form_for(current_user.relationships.build(followed_id: @chef.permalink),
             remote: true) do |f| %>
  <%= f.hidden_field :followed_id %>
  <div class = "org_btn"><%= f.submit "FOLLOW"%></div>
<% end %>
